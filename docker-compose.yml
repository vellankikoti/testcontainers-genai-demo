services:
  db:
    image: postgres:latest
    container_name: postgres-db
    environment:
      POSTGRES_USER: testuser
      POSTGRES_PASSWORD: testpass
      POSTGRES_DB: testdb
    ports:
      - "5432:5432"
    volumes:
      - ./db-init:/docker-entrypoint-initdb.d
    healthcheck:  # Optional, to monitor manually
      test: ["CMD-SHELL", "pg_isready -U testuser -d testdb -h localhost"]
      interval: 10s
      timeout: 5s
      retries: 5

  backend:
    build:
      context: ./backend
    container_name: backend
    ports:
      - "5000:5000"
    environment:
      OPENAI_API_KEY: ${OPENAI_API_KEY}
      DATABASE_URL: "postgresql+psycopg2://testuser:testpass@postgres-db:5432/testdb"
    depends_on:  # Basic dependency to ensure `db` is started before `backend`
      - db
    command: >
      sh -c "chmod +x /app/wait-for-it.sh &&
      /app/wait-for-it.sh postgres-db:5432 -- python app.py"
    volumes:
      - ./backend:/app

  frontend:
    build:
      context: ./frontend
    container_name: frontend
    ports:
      - "80:80"
    depends_on:  # Ensure `backend` starts before `frontend`
      - backend
    environment:
      REACT_APP_API_BASE_URL: "http://backend:5000"
    volumes:
      - ./frontend:/app
